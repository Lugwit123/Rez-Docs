# 相册功能与数据模型（l_WChat）

状态：已实现并实测，截至 2026-09-26。代码：`l_WChat/999.0/src/l_WChat/api/routes.py`（相册段）、`templates/album.html`、`static/upload_direct.js`。
（2026-09-26 增补：🏷 **AI 打标**＝百度智能云识图，手动批量、不自动，见 §7；网盘侧实现在 `lugwit_baidu_netdisk` 的 `vision.py` 与 `POST /api/vision/tag`。）
相关：`Rez_pkg/lugwit_baidu_netdisk.md`（上传/移动/白名单）、`Rez_pkg/lugwit_baidu_netdisk.md` §18（服务端上传改动）。

---

## 1. 三条硬规则（2026-09-26 定）

| 概念 | 规则 |
|---|---|
| **相册** | 只是一个**容器**。照片归属唯一；相册**只手动新建**（相册管理面板），上传只能从某个相册标题上的 `＋` 进；**不再按日期自动命名** |
| **分组（视图）** | 只是**保存下来的条件**。不建相册、不建目录、不搬文件；一张照片可命中多个视图。`and / or / not` 可嵌套 |
| **月龄** | **不再是一个相册维度**。用内置视图 `宝宝月龄（自动）`（条件 `age = 当前月数`）表达，也可自建 `age >= 2` 之类 |
| **「其他」（保留相册）** | 删相册时的**收容所**：照片挪进它，不丢。**没有就自动新建**（常驻显示）、**不能删、不能改名、不能手动新建同名**（`group_key=9995`） |

> 曾经的「宝宝 N 个月 / 夫妻合照 自动组」已撤：只剩「夫妻合照」作为内置相册名，其余相册一律自建。

---

## 2. 存储模型：一张照片有三处落点

| 落点 | 路径 | 说明 |
|---|---|---|
| 本地副本 | `~/.lugwit/l_WChat/data/album/<stored>` | 服务器侧；缩略图/马赛克/AI 分析吃这份。手机直传的照片首次被看时才回源落盘 |
| 云盘（files 域） | `/apps/Lugwit/l_wchat/album/<相册名>/<日期_原名>` | 百度网盘裸文件；可直接在网盘客户端里看到 |
| 版本库（depot） | `/l_wchat/album/<相册名>/<日期_原名>` | blob 模式：**move/delete 只改数据库记录**，`version_depot/blob/**` 一个字节不动，历史 rev 可回滚 |

相册目录下的元数据（都在本地 `album/`）：

| 文件 | 内容 |
|---|---|
| `_index.json` | 照片索引（见 §3） |
| `_groups.json` | `order` 显示顺序、`custom` 自建相册、`pinned` 常驻（空相册也显示） |
| `_views.json` | 保存的视图：`[{name, expr}]` |

---

## 3. 索引字段（`_index.json` 每一条）

| 字段 | 说明 |
|---|---|
| `stored` | 本地文件名 = `<32hex>.<ext>`，接口 `url = /api/album/file/<stored>` |
| `original` / `uploaded_at` / `size` | 原文件名 / 上传时间 / 字节数 |
| `group` / `group_key` | 展示相册名 + 排序键（`>=0` 月龄、`9999` 夫妻合照、`9998` 未分组、`9997` 普通相册、`9995` 其他） |
| `remote_dir` / `remote_name` | **物理**云目录与云文件名（删相册后展示组会变，物理位置不会，直链解析按它走） |
| `timeline_date` | 成长记录时间标记（`YYYY-MM-DD`） |
| `tags` | 视图条件用；由「🏷 AI 打标」写入（百度智能云图像识别的关键词，手动批量、不自动），也可在大图里**手动增删**（同一个字段） |
| `vision` | 那次打标的原始结果 `{tags:[{keyword,score,root}], model, cached, tagged_at}`，便于重打或调阈值 |
| `deleted` / `deleted_at` / `trash_from` / `trash_remote_dir` / `trash_cloud_moved` | 回收站状态 |

---

## 4. API 一览（前缀 `/api`，l_WChat 1234）

| 接口 | 用途 |
|---|---|
| `GET /album/photos` | 相册分组视图；`?view=<名>` 则返回该视图命中的平铺照片 |
| `POST /album/upload` | `multipart`：`file` + `category`（baby/couple）+ 可选 `group`（相册名，自动登记）。**流式落盘**，500MB 级别不占内存 |
| `DELETE /album/{stored}` | 移入回收站（云盘+depot move） |
| `POST /album/{stored}/mosaic` | 覆盖保存马赛克版本（浏览器 canvas 生成）；覆盖后云端同名文件也被覆盖，depot 堆 rev |
| `POST /album/ai-tag` | 单张 AI 打标：`{stored, content_key(原图 sha256), image_base64}` → 写回该照片的 `tags` + `vision`。额度/QPS 超限返 429（前端据此停止批量） |
| `POST /album/{stored}/tags` | 手动增删标签：`{tags:[...], mode: set\|add\|remove}` → 返回该照片最终 `tags`（与 AI 共用同一字段，`vision` 不动）。标签去空白/去重/最长 24 字/最多 16 个 |
| `GET /album/file/{stored}` | 取图/取视频（FileResponse，带 ETag/Last-Modified/**Range**） |
| `POST /album/groups` · `POST /album/groups/order` · `POST /album/groups/pin` · `POST /album/groups/rename` · `DELETE /album/groups/{name}` | 相册：新建 / 排序 / 常驻开关 / 改名 / 删除。**删除 = 照片挪进「其他」**（云盘+depot `move`，零字节）+ 扫掉目录里没进索引的文件 + 删掉已空的云目录；`未分组`/`其他` 删不动（400） |
| `GET /album/views` · `POST /album/views` · `POST /album/views/preview` · `DELETE /album/views/{name}` | 视图：列表（**数据模型 + 权限位** + 命中数；含两个内置：月龄 / 回收站） / 保存（同名覆盖=编辑；带 `rename_from` 则改名；改内置视图要 `pwd`，`reset=true` 回默认条件） / 试算 / 删除（内置 `deletable=false` → 400） |
| `POST /album/views/builtin/verify` | 只校验内置视图的编辑密码（不落任何东西），给前端闭门用 |
| `GET /album/trash` · `POST /album/trash/restore` · `POST /album/trash/purge` · `POST /album/trash/empty` | 回收站：列表 / 还原 / 彻底删 / 清空 |

---

## 5. 视图条件语法

```json
{"op": "and|or|not", "items": [ ...子条件... ]}
{"field": "time|age|album|ext|size|tag|trash", "cmp": "=,!=,>,>=,<,<=,in,contains", "value": ...}
```

- 叶子字段：`time` 上传日期、`age` 月龄（按出生日期算，可为负=未出生）、`album` 所属相册、`ext` 扩展名、`size` 字节、`tag` 标签列表、`trash` 在回收站（`0`=正常 / `1`=已删除）
- 组合器可嵌套；`not` 语义 = 其 items 全不满足
- 例：`age>=2 AND (tag in [海边,公园] OR ext=png)`、`trash=1 AND ext=mp4`
- **求值源按需放宽**：默认只喂「没删」的行；条件树里**用到 `trash` 才把已删除的行也放进来**（`_album_source_for_expr`）——否则视图永远看不见回收站。`GET /album/views` 的计数、`/album/views/preview` 的试算、`/album/photos?view=` 都走这个规则
- 内置视图有两个（详见 §5.1 的模型 —— 它们就是 `_views.json` 里的普通条目，只是权限位不同）：
  - `宝宝月龄（自动）` = `{"field":"age","cmp":"=","value":<当前月数>}`（**可编辑，要密码**；不可删、不可改名）
    - 密码：环境变量 `LUGWIT_ALBUM_BUILTIN_PWD`，**默认 `123`**；错了 403
    - chip 的 `✏️` → 浏览器 `prompt` 要密码 → 先打 `/album/views/builtin/verify` 验，再预填条件（默认条件那种"单个叶子"也能正确回填）
    - 构造器里的「恢复默认」= `reset=true`，条件回到"月龄 = 当前月数"（随月份自动变）
  - `回收站` = `{"field":"trash","cmp":"=","value":1}`（**同样可编辑、要密码**；不能删、不能改名）
    - 改完（如 `trash=1 AND ext=jpg`）chip 出现 `＊`，但卡片形态不变
    - 命中时网格渲染成**回收站卡片**（`album-photo trash-card`：缩略图 + 🗑 删除时间 + `♻️ 还原` / `× 彻底删除`），统计行里带「清空回收站」
    - 卡片按钮复用回收站接口（`/album/trash/restore|purge|empty`）；面板里的 🗑 按钮仍可用，两条入口同一套逻辑。判断依据是每张照片的 `deleted` 位，不是视图名
- 试算接口给构造器即时反馈（命中数 + 前 8 个样本）

### 5.1 视图数据模型（`album/_views.json` v2）

```jsonc
{
  "version": 2,
  "views": [
    {
      "name": "宝宝月龄（自动）",                  // 显示名，也是接口标识（唯一）
      "expr": {"field": "age", "cmp": "=", "value": 2},   // 条件树 = 这份视图"当前实际用的条件"
      "builtin_key": "age",                       // ""=自建；"age" / "trash"=内置
      "updated_at": "2026-09-26T22:21:14"
    }
  ]
}
```

`GET /album/views` 在模型字段之外再带上**权限位**，前端一律按位渲染，**不再按名字硬编码**：

| 位 | 含义 |
|---|---|
| `kind` / `icon` / `chip_class` / `tip` | `builtin`\|`custom`、chip 图标、附加样式（回收站 = `trash`）、悬停文案 |
| `editable` / `renamable` / `deletable` | 有没有 `✏️` / 能不能改名 / 有没有 `×` |
| `locked` | 改之前要不要密码（内置两个都要） |
| `overridden` | 内置视图的 `expr` 与默认条件不一致（chip 上显示 `＊`） |
| `count` | 命中张数 |

- **内置视图就是普通视图**：首次 `GET` 时按 `BUILTIN_VIEW_SPECS` 把缺的补进 `_views.json`（`_album_views_ensure_builtins`），此后走**同一条编辑路径** —— 同名保存。`builtin_key` 只决定权限位
- 所以 `回收站` 也能改（跟月龄一样要密码、不能删、不能改名）：改成 `trash=1 AND ext=jpg` 之后 chip 显示 `＊`；「恢复默认」按 key 取回 spec 的默认条件（`age` → `月龄=当前月数`，`trash` → `trash=1`）
- 想新建一个叫「回收站」的自建视图 → 被当成编辑该内置视图（要密码）；`DELETE` 内置 → 400
- 前端网格的"回收站卡片"判断用的是**每张照片的 `deleted` 位**，不是视图名 —— 所以视图被改过之后，只要条件还能命中已删除行，卡片照样带 `♻️ 还原 / × 彻底删除`
- **编辑已有视图**：chip 上的 `✏️` 打开构造器（预填名字 / AND-OR / 各行条件，`not` 也跟着回来）；保存 = 同名覆盖，改名走 `rename_from`（旧名字那条被摘掉，不是新增一条）。构造器只画一层，条件里若有嵌套 `and/or`，编辑时会摊平成一层的叶子 + `not`
- `in` 在构造器里输入**逗号分隔**多个值（`海边,公园`），保存时按数组下发
- 识别提示：所有 chip 与相册标题都有悬停 `title`（视图 = 保存的条件 / 相册 = 照片的实际归属 + 云目录 / 回收站 chip = 条件 `trash=1`）

---

## 6. 视频

- 允许扩展名：`mp4/m4v/mov/webm/mkv/avi/3gp`（图片：`jpg/jpeg/png/webp/gif/heic/heif`）
- 网格**不预载视频字节**：显示百度封面（`filemetas` 的 thumbs）+ **中心 ▶**
- 点中心 ▶ = **卡片内直接播放**（`<video controls autoplay playsinline>`，源用百度 dlink 支持 Range，失败回退服务端接口）；按钮变左上角 `✕` 收起；播完自动收起；开灯箱/刷新列表前会先停
- 大文件策略：**≤500MB 原样上传**；**>500MB 客户端压缩**——`<video>` 实时播放 + canvas 逐帧（最长边 1280）→ `captureStream(25)` → `MediaRecorder`（优先 `webm;codecs=vp9,opus`，2.5Mbps），进度按 `currentTime/duration` 实时显示；压缩耗时≈视频时长
- 上传进度条：压缩与上传两段都显示；**>64MB 走 XHR**（拿真实上传进度），≤64MB 仍走 `fetch`（保留手机直传拦截层）
- 视频不参与马赛克（canvas 逐像素只对图片有意义）

---

## 7. AI 打标（百度智能云识图 · 手动批量）

入口：相册页「☑ 批量标记」→ 勾选照片 →「🏷 AI 打标」。**只在手动点击时跑**，上传与同步都不自动打标。
用的是**百度智能云「图像识别」**（`aip.baidubce.com`），与网盘 OAuth 是两套凭据（AK/SK 在网盘首页卡片里填）。

链路（缩图与 sha256 全在浏览器，服务端只转发）：

1. 浏览器从同源 `/api/album/file/<stored>` 取原图 —— 手机直传的照片**顺带触发一次懒回源**；
2. `crypto.subtle.digest('SHA-256')` 算**内容键** → canvas 缩到长边 1024 → JPEG q0.85 的 base64；
3. `POST /api/album/ai-tag`（l_WChat 1234）→ 内部转发网盘服务 `POST /api/vision/tag`（1028）；
4. 网盘侧按内容键查 `vision_tag`：命中直接返回（**零扣费**），未命中才调百度并写缓存；
5. l_WChat 把关键词写进 `_index.json` 的 `tags`，原始结果写 `vision`。

| 项 | 说明 |
|---|---|
| 凭据 | **托管在 lugwit_auth 的中心密钥存储**（PG 里信封加密的密文，KEK 只在 auth 内/机外），命名空间 `model_hub`：字段 `baidu_vision_api_key` / `baidu_vision_secret_key`，provider id `baidu_vision` / `baidu_vision_secret`。读：问 hub `GET /v1/keys/{provider}`（本机回环，密钥明文不跨机）；写：经 hub `POST /keys`。**本机不再有任何明文密钥文件**。入口：网盘首页「🖼️ 图像识别（AI 打标）凭证」卡片（带获取步骤 + 「去获取 Key」直达链接）或 hub 管理台；环境变量 `LUGWIT_BAIDU_VISION_AK` / `_SK`（或 `BAIDU_VISION_API_KEY` / `_SECRET_KEY`）仍最高优先 |
| 额度 | 实名认证后送免费测试额度（接口维度、以智能云控制台为准）；免费档 QPS 2 → 网盘侧进程内串行限速 0.6s/次（留余量，卡 0.5 容易被判超限） |
| 超限行为 | error_code 17/18 → 网盘侧返 429 → 前端**立刻停止**批量并提示（不自动转按量付费）。**若从没在控制台点过「领取免费资源」，QPS 视为 0**：一次调用也报 18，不是代码问题 |
| 过滤 | `score >= 0.3`，最多 5 个关键词（百度本身最多返回 5 个） |
| 缓存键 | 原图 sha256（`sha256:<hex>`）：同图跨相册/重跑只扣一次费；局域网 http 无 `crypto.subtle` 时退回 `stored:<uuid>`（失去复用，结果仍正确） |
| 落库 | `vision_tag(content_key PK, tags text[], raw jsonb, model, created_at)`，与 depot 同一 Postgres |
| 跳过 | 视频、`heic/heif`（浏览器解不了）、取图或解码失败（计入"失败"并给出首个原因） |

代码：`lugwit_baidu_netdisk/999.0/src/lugwit_baidu_netdisk/vision.py`（凭据/token/限速/错误码）、
`web_server.py` 的 `POST /api/vision/tag`、`l_WChat/999.0/src/l_WChat/api/routes.py` 的 `POST /api/album/ai-tag`、
`templates/album.html` 的批量栏按钮与 canvas 缩图。

### 7.1 标签的可见性与手动增删

标签是**照片的属性**，落 `_index.json` 的 `tags`；页面上三处能看到：

| 位置 | 表现 |
|---|---|
| 缩略图左上角 | 最多露 3 个 chip（多的收成 `+N`），空则不占位 |
| 大图（点开照片）下沿 | 🏷 一排 chip，每个带 `×` 去掉；输入框回车或「＋ 加」新增 |
| 信息面板（⋮ → 存储位置） | 「🏷 标签」一行，完整列出 |

- 入口：点缩略图进大图，或 ⋮ → 「🏷 标签」（等价于点开大图）；**批量 AI 打标**打完刷新即可看到 chip。
- 写接口：`POST /album/{stored}/tags`（`mode=set|add|remove`），落盘与 AI 打标同一字段，所以**视图条件 `tag` 立刻可用**。
- 取舍：手动标签与 AI 结果共用 `tags`，**再对该图批量 AI 打标会被 AI 结果覆盖**（`vision` 里的原始结果保留，便于回看）；要长期保留就手动加完别再批量打标。

## 8. 直链与浏览器缓存

- 每张照片额外下发 `thumb_url`（`thumbnail0.baidupcs.com`）与 `direct_url`（`d.pcs.baidu.com`，**URL 里带账号级 access_token**）
- 取链：按**物理云目录**做 1 次目录列表（缓存 5min）+ 1 次批量 `filemetas`（`dlink=1&thumb=1`），直链缓存 **6h**
- 百度给的缓存头实测：缩略图 `max-age=604800`（7 天）、原图 dlink `max-age=259200`（3 天）
- 页面 `<img>/<video>` 直连百度 → **照片字节不过服务器**；跨域无 `ACAO` 所以 JS 读不到字节（马赛克/回退仍需服务端接口）
- 直链过期/失败 → `onerror` 回退 `/api/album/file/<stored>`
- 服务端接口那层由页面用 **Cache Storage**（`album-thumbnails-v1`）显式缓存：命中即返回、不发请求；马赛克保存后主动 `cache.delete(该 url)`（否则会看到旧图）
- **缓存管理面板**（相册管理 → 💾 缓存）：条数/占用、保留 7/30/90 天或不清理、`清理过期`、`清空全部`；索引在 `localStorage["albumCacheMetaV1"]`，打开页面按设置静默淘汰

---

## 9. 已知取舍（别当 bug）

| 现象 | 原因 |
|---|---|
| 删相册后照片出现在「其他」里（物理路径也变了） | 删相册 = 把文件 `opera=move` 到 `/l_wchat/album/其他`（云盘与 depot 各挪一次，**零字节**），索引 `group` 一起改。原来那个云目录空了会被删掉，depot 里旧路径的历史 rev 仍在（`list_dir` 只认"下面还有活文件"的目录，所以不会留幽灵空目录） |
| 索引里没有、但云目录里有文件（历史遗留 / 直接传到网盘） | 删相册时一并扫进「其他」并补登记，**不会漏、不会删** |
| 删相册 ≠ 删照片 | 想真删照片走回收站：单张 `🗑 移入回收站` → 回收站里 `× 彻底删除`。删相册只搬家 |
| 手机直传的照片不进版本库 | 服务器手上没有字节；要进得回源一次（吃服务器流量），暂不做 |
| 打标要先把图送出去 | 浏览器压到长边 1024 再传百度；手机直传的照片第一次打标会触发一次懒回源（吃服务器出向流量） |
| HEIC / 视频没有 AI 标签 | 浏览器解不了 HEIC；视频只有封面缩略图，本次不参与打标 |
| 手动加的标签被 AI 打标覆盖 | `tags` 是同一个字段（视图条件读它）；`vision` 里原始结果仍在，见 §7.1 |
| 额度用尽时打标会「停」 | 百度没有"查剩余额度"的开放接口，只能靠 error_code 17/18 探测后停止（不自动转按量付费） |
| 直链里有 `access_token` | 单用户自用部署的取舍；市场化前必须换签名/代理方案 |
| 手机直传 >64MB 不生效 | 直传层 `GUARD_BYTES=64MB`，大文件回退服务端中转（有进度、但吃服务器流量） |
| 改了 `.py` 没生效 | 热重载有 ~19s 去重窗口，连续编辑会把后一次重启"跳过" → 用 `l_WChat/999.0/dev_restart.bat` 清进程树重启 |

---

## 10. 排错速查

| 症状 | 查什么 |
|---|---|
| 上传返回 403 + "本地路径不在允许范围" | 网盘 `/api/files/upload` 的白名单：`LUGWIT_NETDISK_LOCAL_ROOTS`（默认 `~/.lugwit`） |
| 打标报「AK/SK 未配置」 | 到中心密钥存储填 `baidu_vision_api_key` / `baidu_vision_secret_key`（命名空间 `model_hub`；或从网盘首页 `/baidu/` 的凭证卡填，同一份）；需要智能云「图像识别」应用 + 实名认证才有免费额度。若报「l_model_hub 不可达」，先确认 8462 的 hub 服务在跑 |
| 打标报「额度/QPS 超限」 | 免费测试额度用尽或并发过高：到智能云控制台「图像识别 → 资源列表」看剩余额度；缓存命中的照片不扣费 |
| 图片/视频突然变回服务端加载 | 直链 6h 缓存过期或百度侧改动；刷新页面会重新取链 |
| 云端目录里找不到刚传的照片 | 后台推云是异步任务（`BackgroundTasks`），等几秒；失败看 l_WChat 日志的"网盘服务不可达" |
| 1028 拒连、`netdisk_status` 说 running 但 pid 不存在 | 见 `Rez_pkg/lugwit_baidu_netdisk.md` §18（历史坑：`token_path` 未导入导致启动即崩） |
| 远端相册照片没有直链（`thumb_url` 空、走服务端回源） | 老索引行缺 `remote_dir` / `remote_name`（早期版本没这两个字段）→ 按 `日期_原名` 去云目录里匹配不到 `fs_id` 就补不出直链；新上传的照片自带这两个字段，正常 |

---

## 11. 部署到远端生产机（2026-09-26 实测）

通道 = `l_repo_sync_gui` 的「直推部署」（**不走 git**）：`deploy_channel.DeployChannel` → `121.196.144.88:8764` 的 `/upload_folder`（base64 + `is_binary=True`）→ `/execute` 里 `sha256` 全量校验 → `restart_remote_service` 按「包→端口/别名」映射杀端口重拉。**sha256 不一致不许重启。**

- 本包脚本：`l_repo_sync_gui/999.0/deploy_l_wchat.py`（照 `deploy_l_homepage.py` 写）
  - `python deploy_l_wchat.py --with-netdisk` → 推 l_WChat 5 个文件 → 重启 **1234 `l_wchat_backend`**；推 `lugwit_baidu_netdisk` 整个 `999.0/src/lugwit_baidu_netdisk` → 重启 **1028 `baidu_netdisk_web`**
  - 只推 l_WChat 时省略 `--with-netdisk`
- **为什么不用 GUI 的「推送部署」**：本机改动被「自动上传」提交进 git 后，工作区对 HEAD 干净 → GUI 按 `git status` 差集会判"无差异、跳过"。所以必须写显式文件清单
- **坑（本次踩到）**：网盘包**不能只推 `web_server.py`** —— 它 `from .vision import ...`，远端没有 `vision.py` 就启动即 `ModuleNotFoundError`，1028 永远起不来。该包改成**整 src 目录一起推**（`src_dir`）
- **另一个坑**：`restart_remote_service` 的 `wuwo_dir` 必须传 `<远端根>/../wuwo`，传空会 `Popen(cwd="")` 直接抛
- 回验（远端 `/execute`）：`GET 127.0.0.1:1234/api/album/views` 应回两个内置视图（`builtin_key=age|trash`）→ 经 nginx `http://<host>/l_wchat/api/album/views` 也 200；`netstat` 里 1028/1234 都要有监听
