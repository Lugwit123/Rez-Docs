# 密钥落点与红线（唯一速查）

状态：**2026-09-26 逐条实测核对**。改任何凭据 / 密钥相关代码前先读这一页。
细则与设计理由见 [`lugwit_auth统一用户授权服务设计.md`](lugwit_auth统一用户授权服务设计.md)
（§5.3 D 类、§12 D4–D8、**P4.6 密钥集中治理**）；模型侧的落地进展见该文档 P4.6 附录。
下文提到代码一律用**函数名/字段名**（行号会漂），行号截至 2026-09-26。

---

## 1. 三条铁律

1. **密钥与数据分离**：密文可以随数据走（PG / Depot）；**KEK（主密钥）与签名私钥既不进 PG 也不进 Depot**。
2. **除 `lugwit_auth` 外，全仓禁止自造加密、禁止自持密钥**（禁 `import lugwit_auth.secret_store`、禁自造 Fernet）。
   共享秘密走 auth 的 HTTP：`/api/v1/secrets`（存中心）或 `/api/v1/crypto/wrap|unwrap`（密文留自己库）。
3. **密钥不进会移动的东西**：源码树、目录推送同步的目录、Depot 提交、备份 dump、镜像与客户端分发物。

---

## 2. 落点一览（实测）

| # | 内容 | 位置 | 形态 | 谁读 |
|---|---|---|---|---|
| 1 | **各厂商模型 key + 百度识图 AK/SK**（登记 13 个 provider；实测已配置 12） | **PG `credentials` 表**，`scope='model_hub'` | **密文**：`secret_enc` + `dek_wrapped` + `kek_id` | `l_model_hub.keys`（60s 缓存）→ 同机消费方走 hub `GET /v1/keys` |
| 2 | **KEK**（包 DEK 的主密钥） | `~/.lugwit/lugwit_auth/master.key`（另 `master.key.prev`、`keys/*.key` 历史环） | Windows：`DPAPI1\n` + `CryptProtectData`（绑当前用户+机器）；其它平台 `RAW1\n` + 0600 | **只有 auth 进程** |
| 3 | JWT 签名私钥 / 公钥 / JWKS | `~/.lugwit/lugwit_auth/jwt_private.pem` / `jwt_public.pem` / `jwks.json` | PEM / JSON | auth 签发；各服务验签（可离线读公钥） |
| 4 | hub 的服务凭据（admin01 账号口令） | `~/.lugwit/l_model_hub/auth.json` | JSON，0600 | `l_model_hub.auth_secrets` |
| 5 | **PG 连接串（含库密码）** | `~/.lugwit/lugwit_auth/db.json`（`{"url": …}`，0600）或 env `LUGWIT_AUTH_DB_URL` | JSON / env | auth、depot（`require_db_url()`） |
| 6 | 百度网盘 OAuth（client_id/secret/sign_key） | `~/.lugwit/baidu_netdisk/baidu_netdisk_credentials.local.yaml` | YAML | `lugwit_baidu_netdisk` |
| 7 | 百度网盘 token（access/refresh） | `~/.lugwit/baidu_netdisk/baidu_netdisk_token.local.json` | JSON（自动刷新会重写） | 网盘包 |
| 8 | 百度识图 access_token（30 天，**派生值**） | `~/.lugwit/baidu_netdisk/baidu_vision_token.local.json` | JSON | `vision.access_token()` |
| 9 | ⚠️ l_WChat 自有配置（7 厂商 key 副本 + 微信 `appsecret` + `lugwit_user/password`） | `~/.lugwit/l_WChat/config.json` | 明文 JSON，0600 | l_WChat（**与 #1 是两份，欠账见 §8**） |
| 10 | ⚠️ l_agent_chat 工作区设置（`lugwit_password` 明文） | `<工作区>/.l_agent_ws/settings.json` | 明文 JSON | l_agent_chat（上传时 `depot_sync._redact` 剔除，不落云端） |
| 11 | l_notepad 站点凭据 | **PG `credentials`**，`scope='l_notepad'` | 密文（同上信封） | 中心存储 |
| 12 | Codemaker hook 信任台账 / agent-secrets | `~/.codemaker/**` | 0600 + 原子写 | 另一信任域，不在本仓治理范围 |

> `~/.lugwit/baidu_netdisk/` 下的 `setting.yaml`、`*.bin/*.tmp`、`src_watch.json`、`restart_guard.json` 是同步配置与运行时残留，**不含密钥**。

---

## 3. 权威存储与读写链路

**模型侧密钥（含百度识图）的权威存储 = auth 的中心密钥存储**，本机不再有对应的明文文件：

```
消费方（l_agent_chat / lugwit_baidu_netdisk.vision / hub 自己）
   └─ HTTP → l_model_hub :8462
        GET  /v1/keys[/{provider}]   取明文：**只允许回环**（handler 核 client.host），跨机 403
        GET  /keys                   掩码摘要 + store_info（需登录）
        POST /keys                   写（需 lugwit token，即网页调用方登录态）
            └─ HTTP + admin01 token → lugwit_auth :1027
                   /api/v1/secrets/model_hub/{key}     ← 读/写/列举/删
                        └─ PG credentials（密文 + dek_wrapped + kek_id）
                               └─ KEK = ~/.lugwit/lugwit_auth/master.key（DPAPI，**永不出 auth**）
```

- **写**：`l_model_hub` 的 `auth_secrets.put_secret()`（admin01 登录；token 只在内存，不落盘）
  / 网盘首页「🖼️ 图像识别（AI 打标）凭证」卡片（经 hub `POST /keys`）
  / 迁移工具 `l_model_hub/tools/migrate_keys_to_auth.py`（默认 dry-run，`--apply` 才动）
- **读**：hub 的 `keys.py`（env > 中心存储，带 `L_MODEL_HUB_KEY_TTL`，默认 60s）；
  **auth/hub 不可达时返回空 + 记录 `last_error`，绝不回退本地明文文件**
- **轮转**：`rotate_master_key.py` 只重包 DEK，业务密文不动

---

## 4. 优先级口径（所有消费方一致）

```
环境变量 <PROVIDER>_API_KEY / LUGWIT_BAIDU_VISION_AK…   ← 最高（部署/CI，不落盘）
  > 中心密钥存储（经 hub，本机 TTL 缓存）
  > 无 —— 明确报错，不回退任何本地明文文件
```

---

## 5. 红线（禁止）

- 在**源码树**里创建或提交密钥类文件：`config.json`、`*.local.yaml`、`*.local.json`、`auth.json`、`db.json`、`master.key`。
- 把密钥放进**会被推送/提交**的目录：目录推送同步的 `src/**`；Depot 提交路径；
  注意 `/api/files/upload` 的白名单默认就是 `~/.lugwit`（上面 #1–#9 全在里面）→ **别整目录上传**。
- 把密码/密钥/token **硬编码进 `.py`**（历史案例：`DEFAULT_DB_URL` 曾硬编码生产库密码，
  2026-09-26 才挪到 `db.json`）。
- KEK / 签名私钥进 PG、Depot、镜像、客户端分发物、聊天或工单。
- 绕过 auth 自造加密（包括"就本地加密一下"的临时实现）。
- 新凭据文件放宽权限（一律 0600）或不补 `.gitignore`。

---

## 6. 允许的例外

- **纯本机、per-user 的缓存**：DPAPI 包裹的本地文件（另一信任域），以及 #7/#8 这类**派生 token**（会过期、可重新获取）。
- **环境变量注入**（`LUGWIT_*`）：首选形态，不落盘。
- **掩码摘要与指纹**可外发：`sk-a...1234`、`kek_id`、`store_info()`（都不含明文）。

---

## 7. 自检与排错

```bash
# KEK 指纹 + 来源 + 密钥环大小（不含密钥）
wuwor lugwit_auth   -- python -c "from lugwit_auth.secret_store import kek_status; print(kek_status())"
# 库连接串来源（不含密码）
wuwor lugwit_auth   -- python -c "from lugwit_auth.config import db_url_source; print(db_url_source())"
# 中心存储可用性 + 已存 key 数
wuwor l_model_hub   -- python -c "from l_model_hub.keys import keystore; print(keystore.store_info())"
# 同机取明文（只回环）/ 掩码摘要（需登录）
curl "http://127.0.0.1:8462/v1/keys?configured_only=1"
curl "http://127.0.0.1:8462/keys"
# 目录落点核对（应只看到 auth.json / db.json / token 缓存，无厂商 key 明文）
dir /b "%USERPROFILE%\.lugwit\l_model_hub" "%USERPROFILE%\.lugwit\lugwit_auth"
```

| 症状 | 先查 |
|---|---|
| 模型调用报"未配置 / key 为空" | hub 在跑？`store_info()["available"]` 与 `last_error`；`~/.lugwit/l_model_hub/auth.json` 在不在 |
| auth 启动即报"未配置用户中心库连接串" | 补 `~/.lugwit/lugwit_auth/db.json` 或 env `LUGWIT_AUTH_DB_URL` |
| 解密失败 / `/accounts` 503 | KEK 失配（多实例各持一把）→ 比 `kek_id`，必要时 `import_kek()` |
| 打标报"l_model_hub 不可达" | 8462 服务是否在跑（`/healthz`） |
| depot / 备份报连接失败 | `require_db_url()` 解析出的串（`db_url_source()`） |
| `git status` 出现密钥文件 | 立刻删 + 补 `.gitignore`；若已提交，按"密钥泄露"处理（轮转） |

---

## 8. 已知欠账（待办）

1. **dev 仍连生产 PG**（`db.json` 当前就是生产串）→ 建独立开发库（设计文档 P4.5）。
2. **5432 公网可达 + 弱密码** `postgres/OC.123456`（开发机能直连上）→ 云安全组只放行运维 IP、
   服务器 `listen_addresses` 收内网 + `pg_hba.conf` 限来源、本地走 SSH 隧道；密码换掉后同步 `db.json`。
3. **`l_WChat/config.json` 的厂商 key 与中心存储是两份**（值可能漂移）→ 按 P4.6 收敛（getter 改走 hub）。
4. **`l_agent_chat` 的 `.l_agent_ws/settings.json` 仍存 `lugwit_password` 明文**。
5. **Depot 提交 / `/api/files/upload` 不做密钥扫描**，而上传白名单默认 `~/.lugwit`（密钥集中地）
   → 加"提交前扫密钥特征"（`BEGIN PRIVATE KEY`/`master.key`/`*_api_key`/`sk-*`）+ 收紧白名单。
6. **`admin01` 口令仅 3 位**，却是"能解开全部厂商 key"的入口 → 换强密码（同步 `~/.lugwit/l_model_hub/auth.json`）。

---

## 9. 变更记录

**2026-09-26（模型侧密钥入库）**

- 新增中心存储客户端 `l_model_hub/auth_secrets.py`（admin01 登录，token 只在内存）。
- `l_model_hub/keys.py` 权威存储改中心存储；`/v1/keys` 回环取明文；`/keys` 返回 `store_info()`。
- 迁移工具 `migrate_keys_to_auth.py`：12 项入库（read-back 校验通过后删明文文件），
  实测 PG 12 行、`dek_wrapped` 全在、疑似明文命中 0、`kek_id` 与本机 KEK 一致。
- `l_agent_chat/config.py::_hub_keys()`、`lugwit_baidu_netdisk/vision.py` 改为经 hub 取。
- **PG 连接串**从 `lugwit_auth/config.py` 硬编码挪到 `db.json` / env（`require_db_url()` fail-closed），
  `l_qframelesswindow/_auth_client/config.py` 的副本同步清理。
- **l_WChat 配置**从包源码目录搬到 `~/.lugwit/l_WChat/config.json`（`paths.CONFIG_PATH`，
  写永不落源码树），并补 `.gitignore`。

**2026-09-22（P4.6 密钥集中治理）**：`/api/v1/secrets`、`/api/v1/crypto`、KEK 严格模式与指纹自检，
详见设计文档 P4.6。
