# 标题栏提供的服务

涉及包：

- `l_qframelesswindow/999.0` — 标题栏库（`L_FramelessMainWindow` + `ServerConfigStore` + `ServerSettingsDialog`）
- `l_script_editor/999.0` — 脚本编辑器组件库（`ScriptEditorTab` 等）
- `l_qt_wgt_lib` — 基础控件（`CodeEditorWidget` 等，Markdown 预览依赖）
- 使用方示例：`l_notepad/999.0`（登录按钮、服务器设置、脚本编辑器、帮助文档全接入）

## 1. 背景与目标

多个程序共用同一条无边框标题栏（`L_FramelessMainWindow`）。与其让每个程序各自实现
登录、服务器配置、脚本编辑等重复能力，不如**把通用服务下沉到标题栏**：

- 使用方只需继承 `L_FramelessMainWindow`，再按需注册数据源 / 回调，即可获得整套能力
- 服务所需的服务器地址统一走 nginx 8080 统一入口（见《Nginx反向代理机制》），
  后端端口（1027/1028/1026/8765）不对外暴露
- 登出、改服务器等操作通过信号通知使用方，标题栏不持有业务数据

## 2. 服务总览

| 服务 | 标题栏入口 | 使用方接入 | 依赖 |
|------|-----------|-----------|------|
| 登录服务（认证接入） | `authUrl()` / `authRoute()` / `serverConfigChanged` 信号 | 自建登录按钮读取配置 | 认证服务 1027（nginx `/api/v1/` 转发） |
| 服务器设置 | `🌐` 按钮 + 设置对话框 | `setServerConfigStore(store)` | 无（本地持久化） |
| 脚本编辑器 | `</>` 按钮 | `setScriptEditor` / `setScriptEditorHandler` | `l_script_editor`（本地 HTTP 8764） |
| 帮助文档 | `?` 按钮 | `setHelpDocument` / `setHelpDocumentDir` | 无（本机 .md 文件） |
| 重启 | 重启按钮 | `restart_program()` | 本机进程 |
| 刘海下拉面板 | 标题栏中央刘海（悬停展开） | `addNotchButton` / `setNotchDropdownWidget` | 无（本机） |

## 3. 登录服务

标题栏**不弹登录框、不存账号**——它提供的是"认证接入能力"：统一维护认证服务地址与
路由，使用方拼出登录等端点即可：

- `auth_url`：认证服务地址，默认 `http://121.196.144.88:8080`（nginx 8080 入口，
  `location /api/v1/` 转发到认证服务 1027）
- `auth_route`：认证路由，默认 `/api/v1/auth`
- 端点拼接规律：登录 / 刷新 token / me 等端点拼在 `auth_url + auth_route` 之后，
  即 `http://121.196.144.88:8080/api/v1/auth/login`
- 生效值三层解析：**包覆盖配置 > 环境变量 > 标题栏默认**（见第 4 章）
- 配置变更时标题栏发出 `serverConfigChanged(dict)` 信号，使用方据此刷新登录态

以 l_notepad 为例（标题栏右侧自带"登录"按钮）：

```
点击「登录」 ──► LoginDialog（用户名 + 密码）
                  │  POST {auth_url}{auth_route}/login  ──► nginx ──► 认证服务 1027
                  ▼
               拿到 token ──► 应用登录（账号收藏切到 HTTP API、持久化 token）
已登录再点「登录」──► 登出
右键「登录」按钮 ──► 「🌐 服务器设置...」（打开第 4 章的对话框）
```

```python
# 登录请求示意（l_notepad LoginDialog 的实际用法）
base = self.authUrl() + self.authRoute()          # http://...:8080/api/v1/auth
resp = requests.post(base + "/login",
                     json={"username": uname, "password": pwd})
token = resp.json()["token"]                       # 成功后凭 token 调业务 API
```

其他配套：

- **本机免登录**：`POST {auth_url}/api/v1/auth/auto`（Auth Service 本机免登录通道）
- **token 失效**：业务层发现凭据过期后发 `login_expired` 信号，主窗口清除登录状态
- 后端鉴权由 `lugwit_auth` 中间件拦截未登录请求（返回 401），标题栏不参与校验

## 4. 服务器设置服务

`setServerConfigStore(store, show_button=True)` 注入 `ServerConfigStore` 后，
标题栏结束区出现 `🌐` 按钮；点击打开 `ServerSettingsDialog`（模态）。
使用方已有自定义入口时传 `show_button=False`（l_notepad 就是把入口放在登录按钮右键菜单里），
自行调用 `showServerSettingsDialog()` 打开。

### 4.1 对话框交互

| 步骤 | 状态 | 行为 |
|------|------|------|
| 1 | 打开即只读 | 只读展示**当前生效值**（包覆盖 / 环境变量 / 标题栏默认三层解析结果），无论有没有覆盖配置，复选框一律默认不勾选；存在覆盖配置时提示首行说明字段展示的是覆盖值（非标题栏默认 121.196.144.88），避免「为什么不是默认 IP」的困惑 |
| 2 | 本机模式开关 | 勾选后四个字段临时切到 `127.0.0.1`（`auth_url/api_url/log_server_url` 变为 `http://127.0.0.1:8080...`），取消勾选恢复服务器值；用于本地联调 |
| 3 | 覆盖复选框 | 勾选"覆盖标题栏提供的服务器设置"后输入框才可编辑 |
| 4 | 保存 | 勾选保存 = 写入包自己的覆盖配置；不勾选保存 = 清除覆盖，恢复标题栏提供值 |
| 5 | 配置文件行 | 窗口底部展示本地配置文件路径 + 存在 / 未创建状态（文本可选中便于复制）；「编辑文件」按钮用记事本打开该文件（不存在时先创建空配置 `{}`），关闭记事本后表单 / 提示 / 路径状态自动重新加载 |

### 4.2 配置保存位置与优先级

覆盖配置保存在 **包的数据目录**（wuwor `config.yaml` 的
`data_dir: "{user}/.Lugwit/{pkg}"` 模板决定）：

```
C:\Users\<user>\.Lugwit\<包名>\server_config.json
例：C:\Users\wb.fengqingqing\.Lugwit\l_notepad\server_config.json
```

读取优先级：**持久化（包覆盖）> 环境变量 > 标题栏默认**。

### 4.3 ServerConfigStore API

| 方法 | 说明 |
|------|------|
| `auth_url()` / `auth_route()` | 认证地址 / 认证路由（三层解析结果） |
| `api_url()` / `log_server_url()` | 业务 API / 远端日志服务地址 |
| `all()` | 当前完整生效配置 dict |
| `save(**kwargs)` | 写入覆盖配置（合并后持久化） |
| `clear(keys=None)` | 清除覆盖（全部或指定键），恢复标题栏默认 |
| `has_persisted()` / `persisted_keys()` | 是否已有覆盖 / 覆盖了哪些键 |
| `config_file()` | 配置文件路径 `<data_dir>/server_config.json`（对话框配置文件行展示、编辑按钮使用） |

保存/清除后标题栏发出 `serverConfigChanged(dict)`，使用方（如日志模块）按新地址重启连接。

## 5. 脚本编辑器服务

标题栏 `</>` 按钮（`scriptEditorButton`），**单击 / 双击语义不同**：

| 操作 | 行为 |
|------|------|
| 单击 | 启动脚本编辑器的 **HTTP 远程执行服务**，按钮文本变为 `</> :8764`；已在运行则保持 |
| 双击 | 打开脚本编辑器窗口（或按嵌入方式激活） |

### 5.1 ScriptEditorTab（编辑器本体）

- Python 代码编辑：语法高亮 + 行号 + 自动补全
- 执行选中代码或全部代码（`Ctrl+Enter`）；执行外部 `.py` 文件
- 多代码 Tab 管理（新建 / 关闭 / 重命名）
- 会话自动保存 / 恢复；`Ctrl+滚轮` 缩放
- 智能清空代码（保留 import 语句）；调试模式 / 快捷转换开关

### 5.2 HTTP 远程执行服务

Bottle 内嵌（vendored，零外部依赖），后台线程监听，允许外部客户端远程提交代码
在编辑器执行环境中运行：

```bat
:: 提交代码执行
curl -X POST http://127.0.0.1:8764/execute -H "Content-Type: application/json" -d "{\"code\": \"print('hello')\"}"

:: 最简调用：直接执行 .py 文件（路径放 URL 查询参数，允许空格/中文）
curl "http://127.0.0.1:8764/execute?path=D:/my dir/中文脚本.py&timeout=300"
```

- 响应 JSON：`{"success", "stdout", "stderr", "result", "error"}`
- 端口：默认 `8764`，合法区间 `[8700, 8764]`，环境变量 `SCRIPT_EDITOR_HTTP_PORT`
  可覆盖（多实例并行各配一个端口）
- 并发限制：最多同时 `SCRIPT_EDITOR_MAX_INFLIGHT`（默认 8）个 `/execute` 请求
- 跨线程架构：Bottle（后台线程）→ 发射 Qt 信号 → 主线程执行脚本 → 写回结果唤醒请求线程
- 状态同步：`http_server_state_changed(running, port)` 信号驱动按钮文本
- 配套：`l_agent_chat` 反向代理 `/chat`（`L_AGENT_CHAT_PROXY_URL`，默认
  `http://127.0.0.1:1250`），把聊天网页及其绝对 API 路径重写为 `/chat` 前缀

### 5.3 Agent 工具系统

脚本执行环境预注入 `agent` / `tools` 命名空间，脚本或外部 agent 可直接调用：

```python
tools.list_tools()              # 列出所有可用工具
tools.call("read_file", "D:/x.py")   # 按名称调用（agent 主入口）
tools.read_file("D:/x.py")      # 属性式调用

@tools.register_tool("double", description="翻倍",
                     parameters=[{"name": "n", "type": "number", "required": True}])
def _double(n):
    return {"result": n * 2}
```

- 默认工具集：文件读写 / 目录 / 搜索 / 命令执行 / HTTP / 环境变量 / 编辑器控制
- HTTP 服务 `GET /tools` 提供工具发现接口（供外部 agent 拉取 schema）

### 5.4 三种嵌入方式

| 方式 | 接口 | 适用场景 |
|------|------|---------|
| 默认独立窗口 | 不注册 | 简单场景，双击 `</>` 弹出 900x600 窗口 |
| 懒加载工厂 | `setScriptEditorHandler(handler)` | 编辑器嵌入主界面 QTabWidget，点击时创建并交给 `handler(editor)` |
| 预加载实例 | `setScriptEditor(editor, activate)` | 主程序初始化已嵌入编辑器；`activate` 为点击时激活对应 tab 的回调 |

## 6. 帮助文档服务

标题栏 `?` 按钮（`helpButton`），未设置文档时自动禁用：

| 接口 | 模式 | 弹窗 |
|------|------|------|
| `setHelpDocument(md_file_path)` | 单文件 | `_HelpDocumentDialog`（Markdown 预览） |
| `setHelpDocumentDir(dir_path, recursive=False)` | 目录浏览 | `_HelpDocumentBrowserDialog`（左侧文件列表 + 右侧预览） |

渲染由 `l_qt_wgt_lib` 的 `CodeEditorWidget.show_markdown_preview()` 完成，
只读展示，带字号缩放（Ctrl+滚轮）工具栏。

## 7. 其他内置能力

### 7.1 刘海下拉面板

标题栏中央的"刘海"（notchWidget）悬停展开下拉面板，内置固定按钮：

- `📷` 截取窗口自身
- `⚡` 性能调试（`_PerfDebugDialog`：跳过整窗强制重绘 / 禁用横竖布局切换 /
  跳过刘海重定位 / 跳过标题栏宽度同步 / 输出 resize 耗时到日志，勾选即时生效）
- `🔍` 组件检查模式（`Ctrl+右键` 任意组件弹出右键菜单，复制其布局与名称；ESC 退出）

扩展接口：`addNotchButton(button)` 追加按钮（5 行 3 列网格）、
`setNotchDropdownWidget(widget)` 挂自定义面板、`setNotchDropdownSize(w, h)` 调尺寸。

### 7.2 重启

`restart_program(parent, show_confirm)`（`restart.py`）：

- 用独立进程重新启动当前程序（自动保留 `python -m` 启动方式，避免相对导入失败）
- 同时启动独立**监控进程**验证新进程启动状态；即使原进程崩溃，监控进程也能
  检测到异常并弹窗提示

### 7.3 窗口 / 标题栏基础能力

- 置顶按钮（pin）、最小化 / 最大化 / 关闭系统按钮
- 响应式布局：窗口宽度低于阈值（默认 500px）自动切换横排 / 竖排
- 组件 API：`addLeftWidget` / `addRightWidget` / `addStartWidget` / `addEndWidget`、
  `setStartWidgets` / `setEndWidgets`、`setContentWidget`
- 图标按钮：`hideTitleBarIcon` / `showTitleBarIcon`、`setIconContextMenuCallback`
  （右键菜单回调，l_notepad 用它放"显示/恢复、最小化到托盘、设置、重启、退出"）

## 8. 使用方接入示例（最小接入）

```python
from qframelesswindow import L_FramelessMainWindow
from qframelesswindow.server_config import ServerConfigStore

class MyWindow(L_FramelessMainWindow):
    def __init__(self):
        super().__init__()
        # 1) 服务器设置 + 认证配置（登录服务的基础）
        self.setServerConfigStore(ServerConfigStore(data_dir=my_data_root))
        # 2) 帮助文档（目录模式）
        self.setHelpDocumentDir(r"D:\path\to\doc")
        # 3) 脚本编辑器嵌入主界面 tab
        self.setScriptEditorHandler(
            lambda editor: self.editor_tabs.addTab(editor, "脚本"))

    def login(self, username, password):
        # 4) 登录：读标题栏维护的认证配置拼端点
        import requests
        base = self.authUrl() + self.authRoute()
        resp = requests.post(base + "/login",
                             json={"username": username, "password": password})
        return resp.json()["token"]
```

## 9. 已知注意事项

- 服务器设置只影响**本机使用方**的配置（写入 `~/.Lugwit/<pkg>/server_config.json`），
  不改服务器任何东西；换机器需重新设置
- 默认指向生产服务器 `121.196.144.88:8080`（nginx 统一入口）；本地联调用对话框
  "本机模式"开关临时切换，联调完**取消勾选覆盖再保存**即可恢复标题栏提供值
- 脚本编辑器 HTTP 服务（8764）是**本机服务**，与服务器无关，防火墙无需放行；
  端口被占用时可用 `SCRIPT_EDITOR_HTTP_PORT` 换端口
- 登录服务默认不拦截密码之外的业务流量——业务 API 鉴权由各后端自行处理
- 新程序接入时若不需要某服务，不调用对应注册接口即可（帮助按钮自动禁用、
  `</>` 按钮单击时提示 `l_script_editor 未安装`）